version "4.0"

class ShaderTimer : EventHandler
{
    override void RenderOverlay(RenderEvent e) 
    {
        float timer = (gametic + e.FracTic) * 1 / 35;    
        Shader.SetUniform1f(players[consoleplayer], "colorize", "timer", timer);
    }
}
